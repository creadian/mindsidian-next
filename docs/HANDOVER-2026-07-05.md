# HANDOVER — Mindsidian NEXT, evening 2026-07-05

**For any Claude session picking this up. Read fully before touching the repo.**

## ⚠️ Prime rule from Christian (2026-07-05, ~23:00)

**Do NOT delete, revert, rebase, or overwrite ANY of the latest commits.**
A chat rewind lost conversation context, but the repo is the source of
truth and its history is complete and correct. Current HEAD `0342c5d`
("Bump to 2.0.0-alpha.6") and everything before it = valuable, verified
bug fixes and features. If a future chat context seems to contradict the
git log, THE GIT LOG WINS.

## State (all verified this evening)

- Version **2.0.0-alpha.6**, committed locally, 131/131 tests, build clean.
- Deployed + reloaded in the `Claude_testing` vault; live sanity passed
  (no-edit open/close byte-identical, edits save, zero console errors).
- Working tree CLEAN at handover time.
- **NOT yet pushed to GitHub** — 19 commits ahead of origin/main.
  Push + `gh release create 2.0.0-alpha.6` (assets: main.js,
  manifest.json, styles.css; release notes drafted in the session) are
  ready but BLOCKED pending Christian's explicit "yes, push and release".
  Never push or release without that explicit approval.

## What alpha.3 → alpha.6 contains (see docs/06-build-log.md for detail)

- alpha.3: triple-audit data-safety round (15 fixes, Codex sign-off).
- alpha.4: desktop zoom rate + reveal-on-add; mobile F1 swipe shield /
  F2 nearest-rect drop search / F3 bar offset + scale fix.
- alpha.5: arrow-key reveal, keyboard-adjacent bar, branch-delete
  confirm, colored palette swatches, "Node style" setting.
- alpha.6: momentum glide (touch), bar hugs keyboard while typing
  (mn-kb-up), delete confirm for ALL deletes.

## Open items (in order)

1. Christian's explicit approval → push + GitHub release (BRAT).
2. Christian's iPhone test — 10-point F1 checklist at the end of
   docs/06-build-log.md, plus glide / bar / delete-confirm / drop-arrows.
3. After phone sign-off: discuss v1→v2 adoption plan for Brain Forest
   (backup first; NEVER enable v1+v2 on the same vault).

## Scheduled 2:00 a.m. regression run (if created)

Mission: verify repo intact (HEAD as above, tree clean), re-run
`npm test`, re-run the live data-safety battery against the LT-* files
in Claude_testing (fold roundtrip, no-edit open/close byte-identity,
edit save, EC10a normalization, external-change handling), and write a
short status into this file under a "## 2 a.m. run result" heading.
STRICTLY non-destructive: no push, no release, no version bump, no
reverts. Test files LT-01…LT-13 in Claude_testing are disposable.

## Checkpoint run result (2026-07-06 ~12:20, after token-window interruption)

Repo verified INTACT: HEAD `0342c5d` (alpha.6), tree clean, nothing
reverted; deployed plugin = alpha.6. `npm test` 131/131. Live battery
ALL PASS: no-edit open/close byte-identical (canary + created with
markmind), fold/unfold roundtrip byte-identical, edit saves, EC10a
leading-dash normalization works, zero console errors.
Still open: (1) Christian's explicit "yes, push and release" → push 20
commits + GitHub release 2.0.0-alpha.6; (2) his iPhone checklist.
